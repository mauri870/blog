# My personal blog

```bash
git submodule update --init --recursive
```

Development with live reload:

```bash
make dev
```

Build with minification:

```bash
make
```

Publish:

```bash
make release
```
